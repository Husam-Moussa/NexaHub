// Auth configuration
const authConfig = {
  // Add your authentication configuration here
  apiUrl: import.meta.env.VITE_API_URL || 'http://localhost:3000',
  // Add other auth-related configuration as needed
};

export default authConfig; 